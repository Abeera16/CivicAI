# URL of the CivicAI FastAPI backend, including the /api prefix.
# When running via the root docker-compose.yml, the browser talks to the
# backend through the host-mapped port, so keep this as localhost:8000.
NEXT_PUBLIC_CIVICAI_API_URL=http://localhost:8000/api
