<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (http://) traffic ONLY to local dev hosts, so you can point
  CIVICAI_BASE_URL at a backend running on your machine (10.0.2.2 on the
  emulator, or your LAN IP on a physical device) without HTTPS during
  development. Remove this file / tighten it before shipping to production
  with a real HTTPS backend.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.2</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
